<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.android.todolist">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar"
        tools:ignore="GoogleAppIndexingWarning">

        <!-- The manifest entry for the MainActivity -->
        <activity android:name="com.junhyeoklee.todolist.ui.view_activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- AddTaskActivity -->
        <activity
            android:name="com.junhyeoklee.todolist.ui.view_activity.AddTaskActivity"
            android:label="@string/add_task_activity_name"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity android:name="com.junhyeoklee.todolist.ui.SettingHelp"></activity>
        <activity android:name="com.junhyeoklee.todolist.ui.SettingPreferenceActivity"></activity>
        <activity android:name="com.junhyeoklee.todolist.ui.SettingColorActivity"></activity>

    </application>

</manifest>